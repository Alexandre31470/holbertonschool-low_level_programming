#include <stdio.h>
/**
* main - Affiche le texte with proper grammar, but the outcome is
* a piece of art followed by a new line
* Return: Always zero
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
    
return (0) ;
}
